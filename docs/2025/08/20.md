---
date: 2025-08-20
next:
  link: /2025/08/21
  text: 败犬日报 2025-08-21
prev:
  link: /2025/08/19
  text: 败犬日报 2025-08-19
title: 败犬日报 2025-08-20
description: 本期日报包含 C++ 标准库实现细节、迭代器行为和内存管理的话题
__tags__:
- title: '## 1. 为什么 `make_shared<A>` 返回类型是 `shared_ptr<_NonArray<A>>`'
  score: 7/10
  tags:
  - C++
  - 标准库特性
  - 泛型编程
  - 编译期
  - 进阶知识
  explanation: 解释 make_shared 返回类型中的 _NonArray 包装器作用，通过 SFINAE 或 concepts 来匹配非数组类型，属于标准库实现细节和泛型编程技巧。
- title: '## 2. unordered_map 迭代器用 prev 是未定义行为吗'
  score: 8/10
  tags:
  - C++
  - STL
  - 基础知识
  - 需改进
  explanation: 说明 unordered_map 迭代器是前向迭代器，使用 prev 函数要求双向迭代器，因此是未定义行为。原解释不够全面，应更明确强调迭代器类别要求。
- title: '## 3. C++ vector 的 push_back 扩容机制为什么不考虑在尾元素后面的空间申请内存'
  score: 9/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - 性能优化
  - 进阶知识
  explanation: 分析 vector 扩容不使用 realloc 的原因，涉及 trivially copyable 限制、移动构造函数调用和 relocate
    语义探讨，属于内存管理和性能优化话题。
---

# 败犬日报 2025-08-20

[[toc]]

## 1. 为什么 `make_shared<A>` 返回类型是 `shared_ptr<_NonArray<A>>`

`_NonArray<A>` 就是 A，作用是通过返回值的 SFINAE 来匹配非数组类型，可以较少代码兼容没有 requires 的版本。实现可能长这样：

```cpp
#if __cpp_concepts && __glibcxx_type_trait_variable_templates
  template<typename _Tp>
    requires (!is_array_v<_Tp>)
    using _NonArray = _Tp;
#else
  template<typename _Tp>
    using _NonArray = __enable_if_t<!is_array<_Tp>::value, _Tp>;
#endif
```

## 2. unordered_map 迭代器用 prev 是未定义行为吗

现象是群友发现本地报错，godbolt 上运行崩溃，这确实是未定义行为。

unordered_map 迭代器是 LegacyForwardIterator，只能向前。

[prev](https://en.cppreference.com/w/cpp/iterator/prev.html) 记载了 BidirIt must meet the requirements of LegacyBidirectionalIterator。

## 3. C++ vector 的 push_back 扩容机制为什么不考虑在尾元素后面的空间申请内存

参考文章：<https://www.zhihu.com/question/384869006>

关键点是，realloc 接口不灵活，它在原地扩张失败后会自动分配内存并 memcpy 过去，不会调移动构造函数。这就必须要保证对象是 trivially copyable 的。

C++ 还没有类似的函数进入标准，可以看 [C++ 中的 relocate 语义 - YKIKO的文章 - 知乎](https://zhuanlan.zhihu.com/p/679782886)
