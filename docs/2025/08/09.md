---
date: 2025-08-09
next:
  link: /2025/08/10
  text: 败犬日报 2025-08-10
prev:
  link: /2025/08/08
  text: 败犬日报 2025-08-08
title: 败犬日报 2025-08-09
description: 本期日报包含两个话题：asan 内存下毒技术和 C++ 与 C 的关系澄清
__tags__:
- title: '## 1. asan 用于下毒的代码'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 编译构建和调试
  - 工程实践
  - 进阶知识
  explanation: 介绍使用 AddressSanitizer 手动下毒内存的技术，用于检测逻辑越界问题，涉及内存管理和调试工具，属于工程实践中的进阶知识。
- title: '## 2. 是否正确：C++ 代码中 int, printf 这些是 C 的东西'
  score: 6/10
  tags:
  - C++
  - 基础知识
  - 内容较主观
  - 需改进
  explanation: 澄清 C++ 中 int 和 printf 等标识符属于 C++ 标准而非 C 语言，但解释过于简单，缺乏详细说明，内容较主观且需要改进。
---

# 败犬日报 2025-08-09

[[toc]]

## 1. asan 用于下毒的代码

```cpp
class AsanPoisonDefer {
#ifdef ADDRESS_SANITIZER
public:
    // Poison the memory region to prevent accidental access
    // during the lifetime of this object.
    AsanPoisonDefer(const void* start, size_t len) : start(start), len(len) {
        ASAN_POISON_MEMORY_REGION(start, len);
    }
    // Unpoison the memory region when this object goes out of scope.
    ~AsanPoisonDefer() { ASAN_UNPOISON_MEMORY_REGION(start, len); }

private:
    const void* start;
    size_t len;
#else
public:
    // No-op for platforms without ASAN_DEFINE_REGION_MACROS
    AsanPoisonDefer(const void*, size_t) {}
    ~AsanPoisonDefer() = default;
#endif
};
```

手动下毒，防止内存上没越界，但逻辑上越界了。

例如访问 `vec[0]` 的时候，给 `vec[1]` 打 poison。

主要用于内存池，越界也不会报错，只能手动下毒。

## 2. 是否正确：C++ 代码中 int, printf 这些是 C 的东西

错误的，都是 C++ 的，只是“恰好”用 C 也能通过编译。
