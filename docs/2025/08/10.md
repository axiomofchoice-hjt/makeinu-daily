---
date: 2025-08-10
next:
  link: /2025/08/11
  text: 败犬日报 2025-08-11
prev:
  link: /2025/08/09
  text: 败犬日报 2025-08-09
title: 败犬日报 2025-08-10
description: 本期日报包含 C++ 成员初始化的话题
__tags__:
- title: '## 1. 怎么用 member init 实现 `S("1234")`'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 内存管理
  - 基础知识
  - 需改进
  explanation: 讨论 C++ 结构体成员初始化问题，涉及 C 风格数组和 std::array 的初始化方式，属于语法特性和内存管理的基础知识。需改进：未提及
    std::string_view 等更现代的解决方案。
---

# 败犬日报 2025-08-10

[[toc]]

## 1. 怎么用 member init 实现 `S("1234")`

```cpp
struct S {
    char code[5];
    S(char (&arr)[5]): code(/*这里应该怎么写*/) {}
};

S("1234");
```

C 风格数组肯定是做不到的，要改成 `std::array`，然后 `std::to_array`。

```cpp
struct S {
    std::array<char, 5> code;
    S(char (&arr)[5]): code(std::to_array(arr)) {}
};
```

当然用 `code{arr[0], arr[1], ...}` 也是可以的。
