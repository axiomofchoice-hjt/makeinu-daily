---
date: 2025-01-13
next:
  link: 2025/01/14
  text: 败犬日报 2025-01-14
prev:
  link: 2025/01/12
  text: 败犬日报 2025-01-12
title: 败犬日报 2025-01-13
description: 本期日报包含 C++ 编程实践、编译器工具、性能优化和硬件相关的多个话题
__tags__:
- title: '## 1. 真实案例之函数参数 `const string &`，直接 `const_cast` 后 `move` 走了'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 工程实践
  - 基础知识
  - 需改进
  explanation: 展示了一个错误的编程实践，通过 const_cast 移除 const 后 move 走字符串，这违反了 const 正确性和内存安全原则，属于基础知识但实现方式错误需要改进。
- title: '## 2. clangd19 没有 requires 关键字的高亮'
  score: 6/10
  tags:
  - C++
  - 编译构建和调试
  - 语法特性
  - 泛型编程
  - 基础知识
  explanation: 讨论 clangd 编辑器对 C++20 requires 关键字的语法高亮支持情况，涉及编译工具和泛型编程特性，属于基础知识。
- title: '## 3. memset benchmark'
  score: 8/10
  tags:
  - C++
  - 性能优化
  - 算法
  - 硬件相关
  - 进阶知识
  explanation: 介绍 memset 和 memcpy 的实现与性能测试，涉及底层内存操作和性能优化，属于进阶知识。
- title: '## 4. simd 128 位提升到 256/512 位什么情况下性能会劣化'
  score: 9/10
  tags:
  - C++
  - 性能优化
  - 硬件相关
  - 专家知识
  explanation: 分析 SIMD 指令集在不同位宽下的性能表现，讨论指令延迟、吞吐量、CPU 降频和对齐等问题，属于专家知识。
- title: '## 5. non-temporal 指令'
  score: 8/10
  tags:
  - C++
  - 性能优化
  - 硬件相关
  - 进阶知识
  explanation: 介绍绕过 cache 直接读写内存的 non-temporal 指令，用于避免 cache 污染，涉及性能优化和硬件特性，属于进阶知识。
- title: '## 6. DMA 用来做什么'
  score: 7/10
  tags:
  - 硬件相关
  - 其他
  - 基础知识
  explanation: 解释 DMA 的基本功能，包括内存与 IO 设备之间以及内存之间的数据传输，不涉及 CPU 占用，属于硬件基础知识。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 真实案例之函数参数 `const string &`，直接 `const_cast` 后 `move` 走了

> ：听说 move 能提高效率 这人怎么不 move？
> ：搞定 怎么提示我 move 无效 说这是 const 的。
> ：找到了 const_cast 可以移除 const 我真是天才。

笑死了。

## 2. clangd19 没有 requires 关键字的高亮

这个功能还没做。

## 3. memset benchmark

<https://github.com/nadavrot/memset_benchmark>

一个 memset, memcpy 的实现和 benchmark。

## 4. simd 128 位提升到 256/512 位什么情况下性能会劣化

不同长度 SIMD 指令的延迟、吞吐不一定相同，如果没处理好就会导致性能降低。

除此之外可能还有 CPU 降频、对齐之类的问题。

群友表示大多数情况性能不会提升。

## 5. non-temporal 指令

绕过 cache 直接读写内存，可以避免其他需要的数据留在 cache 上不被踢出。

## 6. DMA 用来做什么

DMA 可以不占用 CPU 完成内存搬运（但不能加速这个过程）。

DMA 一般在介绍时会带上 IO 设备，但是其实内存之间的搬运也是可以的。
