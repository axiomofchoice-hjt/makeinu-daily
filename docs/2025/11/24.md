---
date: 2025/11/24
title: 败犬日报 2025-11-24
next:
  link: /2025/11/25
  text: 败犬日报 2025-11-25
prev:
  link: /2025/11/23
  text: 败犬日报 2025-11-23
description: 本期日报包含两个 C++ 话题，分别讨论了指针运算的未定义行为和标准库视图算法的潜在性能问题。
__tags__:
- title: '## 1. 为什么 `int a[10]; int* p = a + 11;` 是未定义行为，`int* p = reinterpret_cast<int*>(114514);`
    不是未定义行为'
  score: 9/10
  tags:
  - C++
  - 语法特性
  - 内存管理
  - 专家知识
  - 冷门知识
  explanation: 讨论指针算术与整数到指针转换的未定义行为 (UB) 差异，涉及数组边界和 reinterpret_cast 的规则，属于 C++ 语言中较冷门的专家知识。
- title: '## 2. views::filter + views::tranform 会重复计算 filter pred'
  score: 8/10
  tags:
  - C++
  - 标准库特性
  - STL
  - 函数式编程
  - 性能优化
  - 进阶知识
  - 需改进
  explanation: 讨论 C++20 ranges 中 views::filter 与 views::transform 组合使用时的潜在性能陷阱（谓词重复计算），并提及
    cache_latest 解决方案。属于标准库和函数式编程的进阶知识。标签‘需改进’是因为标题中‘tranform’疑似拼写错误。
---

# 败犬日报 2025-11-24

[[toc]]

## 1. 为什么 `int a[10]; int* p = a + 11;` 是未定义行为，`int* p = reinterpret_cast<int*>(114514);` 不是未定义行为

偷点周刊群话题。

这是加法 UB 了。指针算术是有要求的，大致意思是数组里指针的加减不能移出数组范围 `[begin, end]`（包括末尾元素的后一个）。（[cppref](https://en.cppreference.com/w/cpp/language/operator_arithmetic.html) 里的 Pointer arithmetic 章节）

而整数转换到指针是任何条件都不 UB 的。（[cppref](https://en.cppreference.com/w/cpp/language/reinterpret_cast.html) 里的 A value of any integral or enumeration type can be converted to a pointer type）

***

还有个令人费解的操作，就是 `int* p = a + 11;` 改成 `int* p = reinterpret_cast<int*>(reinterpret_cast<size_t>(a) + 11 * sizeof(int));` 就不是未定义行为了，看似一样的行为实则不然。

（c++ 为什么这么设计，群友没说）

## 2. views::filter + views::tranform 会重复计算 filter pred

得手动加 cache_latest。
