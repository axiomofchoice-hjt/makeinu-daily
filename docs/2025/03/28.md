---
date: 2025-03-28
next:
  link: 2025/03/29
  text: 败犬日报 2025-03-29
prev:
  link: 2025/03/27
  text: 败犬日报 2025-03-27
title: 败犬日报 2025-03-28
description: 本期日报包含 C++ 编程技巧、系统性能分析和软件定价策略等多个话题
__tags__:
- title: '## 1. 测出来 mmap read 比 directio read + userspace pagecache 快'
  score: 7/10
  tags:
  - 性能优化
  - 硬件相关
  - 进阶知识
  - 其他
  explanation: 对比 mmap read 和 directio read 的性能差异，涉及系统级 I/O 优化和缓存机制，属于硬件相关的性能优化话题。
- title: '## 2. QQ 超级会员有多个价格'
  score: 6/10
  tags:
  - 搞笑
  - 其他
  explanation: 描述 QQ 超级会员的差异化定价策略和营销手段，属于程序员趣事类内容。
- title: '## 3. 两个类互相依赖，比如两边的成员函数的参数是对方'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 面向对象编程
  - 基础知识
  explanation: 讲解 C++ 中解决循环依赖的方法，包括前向声明和 pimpl 惯用法，涉及面向对象编程的基础知识。
- title: '## 4. java 为什么可以循环 import'
  score: 7/10
  tags:
  - 其他
  - 基础知识
  explanation: 解释 Java 语言中循环 import 的实现原理，与 C++ 的模块系统进行对比。
- title: '## 5. nvme 持续写入，写了 8G 后 io wait 变得越来越高'
  score: 7/10
  tags:
  - 硬件相关
  - 性能优化
  - 进阶知识
  - 其他
  explanation: 分析 NVMe 固态硬盘在持续写入时的性能问题，涉及 SLC cache 机制和 I/O 调度优化。
---

# 败犬日报 2025-03-28

[[toc]]

## 1. 测出来 mmap read 比 directio read + userspace pagecache 快

write 的话 direct 更好，因为写的时候没有 userspace pagecache，少一层 cache。

mmap read 快可能是因为内核缓存的实现更好（但不知道是不是这个原因）。

## 2. QQ 超级会员有多个价格

个人名片、QQ 群，PC 和移动端也不一样。

还有第一次看到价格之后退出，他会挽留你，给你一个更低价的活动。

太逆天了。

## 3. 两个类互相依赖，比如两边的成员函数的参数是对方

需要前向声明。

```cpp
// 声明类
class A;
class B;

// 定义类，声明成员函数
class A {
    void f(B);
};
class B {
    void f(A);
};

// 定义成员函数
void A::f(B) {}
void B::f(A) {}
```

注意定义函数的时候需要看到完整的类型。

这个问题也能用 pimpl 来做。

## 4. java 为什么可以循环 import

用 C++ 概念来说，java 自己做了头文件和源文件的拆分，事实上 import 的只有头文件那部分。

## 5. nvme 持续写入，写了 8G 后 io wait 变得越来越高

应该是缓外了。

如果没有强制 slc cache 下刷支持，要用 sleep 缓解。
