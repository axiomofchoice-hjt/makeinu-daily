---
date: 2025/12/26
title: 败犬日报 2025-12-26
next:
  link: /2025/12/27
  text: 败犬日报 2025-12-27
prev:
  link: /2025/12/25
  text: 败犬日报 2025-12-25
description: 本期日报包含三个话题，分别涉及 hazard_ptr 内存序、哈希表生产环境性能以及 std::list::size 复杂度变化。
__tags__:
- title: '## 1. hazard_ptr 是不是必须用 seq_cst 来避免 WR 重排？'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 并发编程
  - 专家知识
  - 需改进
  explanation: 讨论 hazard_ptr 实现中内存序 (seq_cst vs acq-rel) 的选择，涉及内存管理和并发编程的底层细节，属于专家知识。需改进：文章仅提出问题，缺乏对标准库描述和
    folly 实现差异的深入分析或结论，内容不完整。
- title: '## 2. 哈希表碰撞问题在生成环境影响大吗'
  score: 8/10
  tags:
  - C++
  - 性能优化
  - 工程实践
  - 进阶知识
  explanation: 讨论哈希表碰撞在生产环境 (特别是高并发、突发流量下) 对性能 (如 P99 长尾) 的影响，属于性能优化和工程实践范畴，是进阶知识。
- title: '## 3. `std::list::size()` 在 C++11 后复杂度变成 O(1)'
  score: 9/10
  tags:
  - C++
  - 标准库特性
  - STL
  - 基础知识
  explanation: 说明 std::list::size 成员函数从 C++98 的 O(n) 到 C++11 及之后变为 O(1) 的复杂度变化，并解释其与
    splice 操作复杂度之间的权衡，涉及标准库特性和 STL，是基础知识。
---

# 败犬日报 2025-12-26

[[toc]]

## 1. hazard_ptr 是不是必须用 seq_cst 来避免 WR 重排？

folly 的实现采用 seq_cst，但是[标准库的描述](https://eel.is/c++draft/saferecl.hp#holder.mem-5)没有这个 seq_cst。

按道理说 acq-rel 是没法建立 RWR 同步的。

## 2. 哈希表碰撞问题在生成环境影响大吗

很大。hash 质量再好，都兜不住大 bucket 冲突问题，生产环境的 burst 流量可能同时打到同一个 bucket 造成部分请求抖动 / 长尾很严重。

上生产的东西，就看平均情况，还有 P99。而且用户体量大了以后，这部份 1% 会被基数放大，影响的是一个巨大群体的用户体验，所以生产不能忍受这种情况。

## 3. `std::list::size()` 在 C++11 后复杂度变成 O(1)

原来是 O(n)，C++11 后可以保存一下 size，并在插入删除时维护一下就可以 O(1) 了。

由于 splice 部分情况下线性复杂度，仍然可以维护 size。（所以 size O(1) 和 splice O(1) 是不能共存的）
