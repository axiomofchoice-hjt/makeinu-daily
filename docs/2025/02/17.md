---
date: 2025-02-17
next:
  link: 2025/02/18
  text: 败犬日报 2025-02-18
prev:
  link: 2025/02/16
  text: 败犬日报 2025-02-16
title: 败犬日报 2025-02-17
description: 本期日报包含 C++ 语言特性、并发编程和内存管理等话题
__tags__:
- title: '## 1. constexpr if 的条件没有 SFINAE'
  score: 8/10
  tags:
  - C++
  - 编译期
  - 泛型编程
  - 进阶知识
  explanation: 讨论 constexpr if 条件不参与 SFINAE 的特性，需要配合 requires 或辅助函数使用，涉及编译期编程和泛型编程，属于进阶知识。
- title: '## 2. 三目运算符的类型'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 类型
  - 基础知识
  explanation: 解释三目运算符类型推导的六个阶段，涉及类型兼容性检查，属于 C++ 语法特性和类型系统的基础知识。
- title: '## 3. 标记访问某个变量时必须持有某个锁'
  score: 9/10
  tags:
  - C++
  - 并发编程
  - 工程实践
  - 进阶知识
  - 编译构建和调试
  explanation: 介绍 Clang Thread Safety Analysis 工具，用于标记变量访问的锁要求，涉及并发编程和工程实践，属于进阶知识。
- title: '## 4. 为什么 cpp 的 mutex 不像别的语言那样能包在变量上呢'
  score: 6/10
  tags:
  - C++
  - 并发编程
  - 内容较主观
  - 基础知识
  explanation: 讨论 C++ mutex 设计哲学，解释其独立语义的历史原因，内容较主观，属于并发编程的基础知识。
- title: '## 5. `shared_ptr<void>`'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - 进阶知识
  explanation: 分析 shared_ptr<void> 的用法和特性，类似于 void* 的共享指针，涉及内存管理和标准库特性，属于进阶知识。
- title: '## 6. `unique_ptr` 的错误用法'
  score: 8/10
  tags:
  - C++
  - 内存管理
  - 工程实践
  - 基础知识
  - 需改进
  explanation: 展示 unique_ptr 的典型错误用法导致 double free，建议使用 make_unique 避免裸指针，涉及内存管理和工程实践，属于基础知识，但示例代码存在潜在问题需改进。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. constexpr if 的条件没有 SFINAE

要套个 requires 或者使用辅助的 SFINAE 函数。

## 2. 三目运算符的类型

<https://zh.cppreference.com/w/cpp/language/operator_other> 详细说明了六个阶段，简单来说是一个尝试兼容三目运算符两个分支的类型；如果两个类型无关，那编译直接不过。

## 3. 标记访问某个变量时必须持有某个锁

Clang 的 Thread Safety Analysis。

<https://clang.llvm.org/docs/ThreadSafetyAnalysis.html>

<https://www.cnblogs.com/jicanghai/p/9472001.html>

还能报告句柄竞争。

```cpp
struct BankAccount {
    Mutex mu;
    int balance GUARDED_BY(mu);
};
```

## 4. 为什么 cpp 的 mutex 不像别的语言那样能包在变量上呢

因为最开始 mutex 语义就是独立的。

## 5. `shared_ptr<void>`

其实就是 `void *` 的共享指针，一些功能和 `void *` 类似。不建议用，因为它具有 `void *` 的很多缺点。

## 6. `unique_ptr` 的错误用法

```cpp
int *rawPtr = new int(42);
std::unique_ptr<int> ptr1(rawPtr);
std::unique_ptr<int> ptr2(rawPtr);
```

经典的 double free。

使用了裸指针就有义务去管理好它的所有权，硬要乱来神仙也救不了。这个问题也可以用 `make_unique` 来避免裸指针。
