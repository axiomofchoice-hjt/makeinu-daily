---
date: 2025-05-09
next:
  link: /2025/05/10
  text: 败犬日报 2025-05-10
prev:
  link: /2025/05/08
  text: 败犬日报 2025-05-08
title: 败犬日报 2025-05-09
description: 本期日报包含 C++ 模板编程、编译器行为、隐私协议和 AI 工具等多个话题
__tags__:
- title: '## 1. `std::bind_front` 相关的模板科技'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 标准库特性
  - 泛型编程
  - 进阶知识
  explanation: 讨论 C++ 中成员函数指针和模板代理的实现，涉及 C++11 到 C++26 的演进，展示模板编程技巧和标准库特性使用。
- title: '## 2. static 模板函数明明用了却报 unused'
  score: 7/10
  tags:
  - C++
  - 编译构建和调试
  - 基础知识
  explanation: 解释模板函数实例化与编译器警告的关系，属于编译器的基本行为理解。
- title: '## 3. QQ 群机器人收集聊天记录可能有隐私问题，最好加群时需要同意隐私协议'
  score: 6/10
  tags:
  - 其他
  explanation: 讨论聊天机器人隐私保护的最佳实践，与 C++ 技术无关。
- title: '## 4. github 上的项目可以用 deepwiki 做成知识库'
  score: 7/10
  tags:
  - 其他
  explanation: 介绍使用 deepwiki 工具将 GitHub 项目转化为知识库的方法。
- title: '## 5. deepwiki 用了 RAG 吗'
  score: 6/10
  tags:
  - 其他
  explanation: 简要确认 deepwiki 可能使用了 RAG 技术。
---

# 败犬日报 2025-05-09

[[toc]]

## 1. `std::bind_front` 相关的模板科技

```cpp
struct Foo {
    void A(int) {}

    template <auto Foo::*func, typename... Args>
    static void proxy(void* pvUser, Args... args) {
        return (static_cast<Foo*>(pvUser)->*func)(args...);
    };
};

int main() {
    Foo foo;
    auto f = Foo::proxy<&Foo::A>;
    f(&foo, 42);  // error: too few arguments to function
}
```

上面的代码 f 被推导成了普通的函数指针 `void (*)(void*)`，所以无法传入 42 这个参数。要解决这个问题，可以把 proxy 改成泛型 lambda 模板：

```cpp
struct Foo {
    void A(int) {}

    template <auto Foo::*func>
    static constexpr auto proxy = [](void* pvUser, auto... args) {
        return (static_cast<Foo*>(pvUser)->*func)(args...);
    };
};
```

但是 C++11 没有泛型 lambda 怎么办？不能直接 auto 了，只能萃取出 `Args...` 的具体类型。

到了 C++26 就简单了，可以直接 `std::bind_front`：

```cpp
#include <functional>
struct Foo {
    void A(int) {}
};

int main() {
    Foo foo;
    auto f = std::bind_front<&Foo::A>();  // 更新：原来的 auto f = std::bind_front(&Foo::A); 是 C++20 写法，可能有额外开销
    f(&foo, 42);
}
```

## 2. static 模板函数明明用了却报 unused

答案是 caller 是个模板函数，没实例化不一定算用。

## 3. QQ 群机器人收集聊天记录可能有隐私问题，最好加群时需要同意隐私协议

败犬日报是纯人工记录的，不涉及。

## 4. github 上的项目可以用 deepwiki 做成知识库

例如群友的问题 duckdb的string类型是如何进行存储的？<https://deepwiki.com/search/duckdbstring_06910986-84f7-450e-8e0f-b32284cc8373>

## 5. deepwiki 用了 RAG 吗

应该是。
