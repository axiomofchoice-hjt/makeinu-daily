---
date: 2025-05-12
next:
  link: /2025/05/13
  text: 败犬日报 2025-05-13
prev:
  link: /2025/05/11
  text: 败犬日报 2025-05-11
title: 败犬日报 2025-05-12
description: 本期日报包含 C++26 新特性、语法细节、类型系统、ABI 差异和社区讨论等多个话题
__tags__:
- title: '## 1. C++26 反射都能干什么'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 元编程
  - 专家知识
  explanation: 介绍 C++26 反射特性的应用场景和示例，涉及语法特性和元编程，属于专家知识。
- title: '## 2. `@#$` 以及反引号在 C++ 有用到吗'
  score: 6/10
  tags:
  - C++
  - 语法特性
  - 预处理和宏
  - 基础知识
  - 需改进
  explanation: 讨论特殊字符在 C++ 中的使用，包含事实错误（$ 作为变量名标准不允许），涉及语法特性和预处理，属于基础知识。
- title: '## 3. `[[]]` 什么时候能自定义'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 专家知识
  explanation: 介绍 C++ 注解（annotation）提案，涉及语法特性扩展，属于专家知识。
- title: '## 4. struct 和 class 的区别'
  score: 9/10
  tags:
  - C++
  - 语法特性
  - 面向对象编程
  - ABI
  - 基础知识
  explanation: 详细说明 struct 和 class 在默认可访问性和 ABI 方面的差异，涉及面向对象编程和 ABI，属于基础知识。
- title: '## 5. 群友用 C++ 写什么'
  score: 5/10
  tags:
  - C++
  - 工程实践
  - 内容较主观
  - 其他
  explanation: 分享 C++ 程序员在不同领域的应用情况，内容主观性强，涉及工程实践。
- title: '## 6. char8_t 与严格别名的问题'
  score: 8/10
  tags:
  - C++
  - 类型
  - 性能优化
  - 进阶知识
  explanation: 分析 char8_t 与严格别名规则的关系，包括性能优化考虑和 C/C++ 兼容性差异，涉及类型系统和性能优化，属于进阶知识。
---

# 败犬日报 2025-05-12

[[toc]]

## 1. C++26 反射都能干什么

<https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2025/p2996r12.html#examples>

## 2. `@#$` 以及反引号在 C++ 有用到吗

`#` 是宏。

`@#` 和反引号刚刚在 C++26 加进来，但是 `@` 被 Objective-C 占了。

`$` 可以作为变量名了。（错误的，标准不允许，只是编译器扩展）

反引号似乎还没用到。

## 3. `[[]]` 什么时候能自定义

annotation 提案：<https://wg21.link/p3394>

## 4. struct 和 class 的区别

只有默认可访问性（public / protected / private），包括成员可访问性和继承时的可访问性。

对于 MSVC 还有 ABI 差异，它们有不同的 mangled name。

## 5. 群友用 C++ 写什么

大多数都是写数据库内核、自动驾驶算法、编译器、芯片、微信的后端等。

QT 和 UE 有自己的一套，和其他方向差别较大。

## 6. char8_t 与严格别名的问题

### 6.1. 严格别名的一个例子

<https://godbolt.org/z/76Gznhd3c>

```cpp
#include <chrono>
#include <iostream>

void sum_with_aliasing(unsigned char* data, int& result) {
    for (int i = 0; i < 1000000; ++i) {
        result += data[0];
    }
}

void sum_without_aliasing(char8_t* data, int& result) {
    for (int i = 0; i < 1000000; ++i) {
        result += data[0];
    }
}
```

unsigned char 可以有别名，所以不能假设 data 和 result 内存不重叠；char8_t 就可以。

### 6.2. 编译器为什么假设 signed char 有别名

为了兼容 C。

C++ 允许别名的类型是 `char` `unsigned char` `std::byte`，C 是 `char` `unsigned char` `signed char`，这里有一些区别。编译器如果不做这个兼容，C 代码直接移植到 C++ 就寄了。

### 6.3. 为什么要有 char8_t

char, unsigned char, signed char 都不能做严格别名优化，只有 char8_t 是独立类型。这样性能会好一些。

<https://www.zhihu.com/question/1904806645999047234/answer/1905396078708228989>

这些类型确实很混乱。
