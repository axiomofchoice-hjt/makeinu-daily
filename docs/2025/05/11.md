---
date: 2025-05-11
next:
  link: /2025/05/12
  text: 败犬日报 2025-05-12
prev:
  link: /2025/05/10
  text: 败犬日报 2025-05-10
title: 败犬日报 2025-05-11
description: 本期日报包含 C++ 参考网站和类型特性的话题
__tags__:
- title: '## 1. `cppreference.cn` 不是正版网站，正版是 `cppreference.com`'
  score: 9/10
  tags:
  - C++
  - 基础知识
  - 需改进
  - 内容较主观
  explanation: 指出 cppreference.cn 是仿冒网站，内容陈旧且翻译有问题，推荐使用正版 cppreference.com。涉及 C++
    参考资源选择，属于基础知识，但内容较主观且需改进（未提供具体翻译问题示例）。
- title: '## 2. `std::decay` 和 `std::remove_cvref` 有什么区别'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 类型
  - 泛型编程
  - 进阶知识
  explanation: 对比 std::decay 和 std::remove_cvref 的类型转换行为，解释前者模拟 auto 推导（会数组/函数退指针），后者仅移除
    cvref 限定，涉及类型系统和泛型编程，属于进阶知识。
---

# 败犬日报 2025-05-11

[[toc]]

## 1. `cppreference.cn` 不是正版网站，正版是 `cppreference.com`

不要用。这个网站的所有者还注册了包括 archlinux.org.cn、gnu.ac.cn、ansible.org.cn、hugging-face.cn 在内的一系列仿冒网站。archlinux 对 archlinux.org.cn 甚至发了[声明](https://www.archlinuxcn.org/notice-regarding-a-similar-wiki-site/)。

而且里面内容很旧，翻译有问题。

## 2. `std::decay` 和 `std::remove_cvref` 有什么区别

只是移除 cvref 限定的话，没区别。（解释一下，cvref 指的是 const volatile 左值右值引用这几个的组合）

`std::decay` 是模拟 `auto x = (?);` 的推导结果，移除了 cvref 限定只是顺手的事，还会导致数组退化成指针、函数退化成指针。

`std::remove_cvref` 就是单纯的移除 cvref 限定。

所以易得它们的区别：

```cpp
static_assert(std::is_same_v<std::decay_t<int (&)[8]>, int*>);
static_assert(std::is_same_v<std::remove_cvref_t<int (&)[8]>, int[8]>);
```
