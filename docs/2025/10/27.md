---
date: 2025-10-27
next:
  link: /2025/10/28
  text: 败犬日报 2025-10-28
prev:
  link: /2025/10/26
  text: 败犬日报 2025-10-26
title: 败犬日报 2025-10-27
description: 本期日报包含 Swift 切片设计、C++ 命名规范、投资新闻和 dynamic_cast 误用等话题
__tags__:
- title: '## 1. swift slice 的反直觉设计'
  score: 7/10
  tags:
  - 其他
  - 基础知识
  explanation: 介绍 Swift 语言中切片操作的拷贝语义和下标规则，属于编程语言基础知识，但非 C++ 内容。
- title: '## 2. 同事写了 __xxx 的自定义函数'
  score: 8/10
  tags:
  - C++
  - 工程实践
  - 基础知识
  - 需改进
  explanation: 讨论 C++ 中双下划线标识符的保留规则和使用 clang-tidy 检查的方法，涉及代码规范，但需要补充更多关于 UB 的具体说明。
- title: '## 3. 可控核聚变火了，上百家投资机构抢份额（文章）'
  score: 6/10
  tags:
  - 其他
  - 内容较主观
  explanation: 分享投资领域新闻，内容主观性较强，与编程技术关系不大。
- title: '## 4. p 是 `void*`，`dynamic_cast<A*>((A*)p)` 是什么神人代码'
  score: 9/10
  tags:
  - C++
  - 面向对象编程
  - 进阶知识
  - 需改进
  explanation: 分析 dynamic_cast 在 void* 转换中的误用和编译器优化行为，涉及多态和类型安全，但需要更详细解释为什么这种用法无效。
---

# 败犬日报 2025-10-27

[[toc]]

## 1. swift slice 的反直觉设计

```swift
var hello = [10, 20, 30, 40, 50, 60]
var foo = hello[2..<5]  // 索引范围是 2, 3, 4
foo[2] = 0
print(hello)  // [10, 20, 30, 40, 50, 60]
print(foo)  // [0, 40, 50]
```

第一点是修改切片会触发拷贝，所以原数组不变。第二点是切片下标是原数组下标。

## 2. 同事写了 __xxx 的自定义函数

双下划线开头都是保留的（给标准库使用），用户使用是 UB。

可以用这个检查 <https://clang.llvm.org/extra/clang-tidy/checks/bugprone/reserved-identifier.html>。

## 3. 可控核聚变火了，上百家投资机构抢份额（文章）

<https://mp.weixin.qq.com/s/XiQ0R0T2Mdx0XUiuzaVDbQ>

## 4. p 是 `void*`，`dynamic_cast<A*>((A*)p)` 是什么神人代码

写的人可能是想检查 p 是不是指向 A 对象。但是这是不行的，编译器认为 dynamic_cast 没用就把它去掉了。

例如 GCC 15.2 `-O3` 的编译结果如下：

```cpp
struct A {
    ~A() {}
};

A* f(void* p) { return dynamic_cast<A*>((A*)p); }

// f(void*):
//     mov     rax, rdi
//     ret
```
