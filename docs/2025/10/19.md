---
date: 2025-10-19
next:
  link: /2025/10/20
  text: 败犬日报 2025-10-20
prev:
  link: /2025/10/18
  text: 败犬日报 2025-10-18
title: 败犬日报 2025-10-19
description: 本期日报包含 C++ 智能指针、模板约束和 requires 语法的话题
__tags__:
- title: '## 1. 有个两年经验的同事显然对 unique_ptr 的理解局限在"如果要用等号连起来需要把后边那个加上 std::move"'
  score: 6/10
  tags:
  - C++
  - 内存管理
  - 基础知识
  - 内容较主观
  - 搞笑
  - 需改进
  explanation: 吐槽同事对 unique_ptr 的理解不全面，涉及移动语义和智能指针使用，属于基础知识但表述较主观，带有搞笑性质，内容不够全面深入。
- title: '## 2. requires 和 SFINAE 移出重载集是在同一个时间'
  score: 7/10
  tags:
  - C++
  - 泛型编程
  - 进阶知识
  - 需改进
  explanation: 讨论 requires 和 SFINAE 在重载决议中的时机问题，涉及模板约束机制，属于进阶知识，但表述不够详细和准确。
- title: '## 3. `requires { expr; }` 和 `requires expr` 的区别'
  score: 9/10
  tags:
  - C++
  - 语法特性
  - 泛型编程
  - 进阶知识
  explanation: 详细解释 requires 表达式和 requires 子句的区别，包含代码示例和参考链接，清晰说明了 C++20 概念语法的使用。
---

# 败犬日报 2025-10-19

[[toc]]

## 1. 有个两年经验的同事显然对 unique_ptr 的理解局限在“如果要用等号连起来需要把后边那个加上 std::move”

逆天。

## 2. requires 和 SFINAE 移出重载集是在同一个时间

## 3. `requires { expr; }` 和 `requires expr` 的区别

`requires { expr; }` 是 requires 表达式 (requires expression)，判断 expr 合不合法，可以得到一个 bool 值。([ref](https://en.cppreference.com/w/cpp/language/requires.html))

`requires expr` 是 requires 子句 (requires clause)，约束模板的语法，检查 expr 是否为真。([ref](https://en.cppreference.com/w/cpp/language/constraints.html))

```cpp
template <typename T>
concept Addable = requires { T{} + T{}; };  // requires 表达式

template <typename T>
    requires Addable<T>  // requires 子句
void f() {}

template <typename T>
    requires requires { T{} + T{}; }  // requires 子句套 requires 表达式
void g() {}

int main() {
    f<int>();
    g<int>();
}
```
