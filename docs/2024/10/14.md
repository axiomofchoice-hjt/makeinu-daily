---
date: 2024-10-14
next:
  link: 2024/10/15
  text: 败犬日报 2024-10-15
prev:
  link: 2024/10/13
  text: 败犬日报 2024-10-13
title: 败犬日报 2024-10-14
description: 本期日报包含 UDP 与 TCP 对比、C++ lambda 捕获、namespace 规范、图像处理、ProtoBuf 实践等多个话题
__tags__:
- title: '## 1. UDP 相对于 TCP 有什么优点'
  score: 8/10
  tags:
  - 其他
  - 基础知识
  explanation: 对比 UDP 和 TCP 的网络传输特性，介绍 UDP 的灵活性和相关协议选择，属于网络基础知识。
- title: '## 2. C++ 的 lambda 没有 const capture'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 基础知识
  explanation: 讨论 C++ lambda 表达式的捕获机制，特别是 const 引用捕获的实现方法，涉及语法特性。
- title: '## 3. namespace std {} 里面加自定义类'
  score: 9/10
  tags:
  - C++
  - 语法特性
  - 基础知识
  - 需改进
  explanation: 明确指出在 std 命名空间添加自定义类是未定义行为，涉及 C++ 语法规范，但解释较为简略需要改进。
- title: '## 4. 如何根据图像模糊程度选不同的压缩比例'
  score: 6/10
  tags:
  - 其他
  - 算法
  - 进阶知识
  - 需改进
  explanation: 提出基于图像模糊程度调整压缩比例的方法思路，涉及图像处理算法，但描述不够具体需要改进。
- title: '## 5. 实现历史遗留的非 ProtoBuf 结构体和 ProtoBuf 结构体互相转换'
  score: 7/10
  tags:
  - C++
  - 工程实践
  - 编译构建和调试
  - 进阶知识
  explanation: 介绍使用 Clang 工具生成代码实现结构体转换，涉及工程实践和编译构建技术。
- title: '## 6. 群友锐评 ProtoBuf'
  score: 6/10
  tags:
  - 其他
  - 工程实践
  - 内容较主观
  - 基础知识
  explanation: 分享对 ProtoBuf 实践中的主观评价，指出内存拷贝等问题，内容较主观但涉及工程实践基础。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. UDP 相对于 TCP 有什么优点

一个肯定的优点是不对传输算法限制，任由上层应用发挥。

传输层可能的各种功能，重排、拆包、阻塞控制、流量控制，UDP 通通都没做，只实现了个端口号。

其他的选择：KCP（可以基于 UDP），QUIC（基于 UDP）

原神使用 KCP + ProtoBuf

## 2. C++ 的 lambda 没有 const capture

C++ 的拷贝捕获（可能的写法是 `[=] { ... }`）是不能修改捕获的变量，除非加了 mutable。

而引用捕获（可能的写法是 `[&] { ... }`）可以修改捕获的变量。

想要达到 const 引用捕获的效果，有两种可行的方法：用局部变量 `const auto &x_ = x; [&x_] { ... }`，或者 `[&x = std::as_const(x)] { ... }`。

## 3. namespace std {} 里面加自定义类

是未定义行为，不要这么做。

## 4. 如何根据图像模糊程度选不同的压缩比例

可以根据不同模糊然后定相似度，然后调压缩比例，二分一个差不多的值。

## 5. 实现历史遗留的非 ProtoBuf 结构体和 ProtoBuf 结构体互相转换

也搞个预处理器生成代码，就像 ProtoBuf 一样。

[ykiko: 使用 Clang 工具自由的支配 C++ 代码吧](https://zhuanlan.zhihu.com/p/669360731)

## 6. 群友锐评 ProtoBuf

实战起来问题老多了，压缩解压都在疯狂拷贝内存。
