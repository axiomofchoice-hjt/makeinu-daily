---
date: 2024-10-10
next:
  link: 2024/10/11
  text: 败犬日报 2024-10-11
prev:
  link: 2024/10/09
  text: 败犬日报 2024-10-09
title: 败犬日报 2024-10-10
description: 本期日报包含 C++ 内存模型和移动构造函数的话题
__tags__:
- title: '## 1. 内存序 acq-rel 和 seq-cst 的区别'
  score: 8/10
  tags:
  - C++
  - 并发编程
  - 内存管理
  - 专家知识
  explanation: 介绍 C++ 内存模型中 acquire-release 和 sequential consistency 的区别，属于并发编程中的高级话题，需要专家知识。
- title: '## 2. 显式删除移动构造函数'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 内存管理
  - 进阶知识
  explanation: 讲解显式删除移动构造函数对编译器行为的影响，涉及 C++ 移动语义和拷贝构造函数的调用规则，属于进阶知识。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 内存序 acq-rel 和 seq-cst 的区别

seq-cst 是最强的内存模型，所有线程里标记为 seq-cst 的原子操作对所有线程都是同一个顺序。

而 acq-rel 只保证了一个线程内的重排关系。

不理解的话可以只用 seq-cst。

[cppref: memory order](https://zh.cppreference.com/w/cpp/atomic/memory_order)

## 2. 显式删除移动构造函数

如果不显式删除移动构造函数，调用时会尝试拷贝构造函数。

```cpp
class Foo {
   public:
    Foo() = default;
    Foo(const Foo& other) {}
};

int main() {
    Foo f1;
    Foo f2 = std::move(f1);  // call Foo(const Foo& other)
    return 0;
}
```

如果调用显式删除移动构造函数（`Foo(Foo&&) = delete;`）就会编译报错。
