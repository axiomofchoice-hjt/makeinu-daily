---
date: 2024-11-26
next:
  link: 2024/11/27
  text: 败犬日报 2024-11-27
prev:
  link: 2024/11/25
  text: 败犬日报 2024-11-25
title: 败犬日报 2024-11-26
description: 本期日报包含两个 C++ 话题：成员类不完整类型编译错误和构造函数属性执行顺序问题
__tags__:
- title: '## 1. member class 不完整类型编译报错'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 标准库特性
  - 内存管理
  - 基础知识
  - 需改进
  explanation: 讨论 std::optional 在成员类不完整类型时的编译错误问题，涉及模板实例化和类型完整性要求。需要改进：缺少对解决方案的完整说明。
- title: '## 2. `[[gnu::constructor]]` 函数执行不保证在全局变量的初始化后'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 编译构建和调试
  - 进阶知识
  explanation: 说明 [[gnu::constructor]] 属性函数的执行顺序问题，提到初始化顺序不确定性和 GCC 的 init_priority
    控制方法。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. member class 不完整类型编译报错

```cpp
#include <optional>

class Test {
   public:
    Test();

   private:
    class Dummy {
        int dummy = 0;
    };

    std::optional<Dummy> dummy_;
};

Test::Test() { dummy_.emplace(); }
```

编译报错，因为实例化 `std::optional<Dummy>` 的时候，Dummy 还是个不完整类型。

<https://stackoverflow.com/questions/75814971/weird-interaction-among-optional-nested-classes-and-is-constructible>

不用 member class 就行了。

## 2. `[[gnu::constructor]]` 函数执行不保证在全局变量的初始化后

所有的初始化顺序都不能保证。

可以用 gcc 的 `init_priority` 控制。
