---
date: 2024-12-09
next:
  link: 2024/12/10
  text: 败犬日报 2024-12-10
prev:
  link: 2024/12/08
  text: 败犬日报 2024-12-08
title: 败犬日报 2024-12-09
description: 本期日报包含 C++ 标准库容器和内存管理的两个话题
__tags__:
- title: '## 1. `std::hive` 容器'
  score: 8/10
  tags:
  - C++
  - 标准库特性
  - 内存管理
  - 进阶知识
  explanation: 介绍 C++26 可能引入的 std::hive 容器，这是一个对象池，支持 O(1) 时间的插入删除和双向迭代，涉及标准库特性和内存管理，属于进阶知识。
- title: '## 2. 一个经典错误：`std::vector` 扩容导致引用失效'
  score: 9/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - 基础知识
  - 需改进
  explanation: 分析 std::vector 扩容导致引用失效的经典错误，涉及内存管理和标准库特性，属于基础知识。需改进：代码示例中的问题描述可以更详细说明未指定求值顺序的具体影响。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. `std::hive` 容器

未来可能进 C++26。

`std::hive` 是一个对象池，支持 $O(1)$ 时间内的插入删除和双向迭代操作，插入删除不会导致其他迭代器失效。

实现原理：块状链表、跳跃域、空闲链表。

推荐阅读：<https://zhuanlan.zhihu.com/p/11456846151>

## 2. 一个经典错误：`std::vector` 扩容导致引用失效

运行结果不符合预期：

```cpp
#include <bits/stdc++.h>
using namespace std;
constexpr int inf = numeric_limits<int>::max();
int main() {
    int n;
    cin >> n;
    vector<vector<int>> tr;
    tr.emplace_back(26, -1);
    vector<int> near{ 0 };
    for (int i = 0; i < n; i++) {
        string s;
        cin >> s;
        int len = s.size();
        int res = inf;
        function<int(int, int, bool)> go = [&](int cur, int matched, bool new_node) {
            if (!new_node)
                res = min(res, len - matched + near[cur]);
            if (matched == len)
                return near[cur] = 0;
            int& nxt = tr[cur][s[matched] - 'a'];
            bool nxt_new_node = false;
            if (nxt == -1) {
                near.push_back(inf);
                tr.emplace_back(26, -1);
                nxt = tr.size() - 1;
                nxt_new_node = true;
            }
            return near[cur] = min(near[cur], go(nxt, matched + 1, nxt_new_node) + 1);
        };
        go(0, 0, false);
        cout << res << '\n';
    }
    return 0;
}
```

`return near[cur] = min(near[cur], go(nxt, matched + 1, nxt_new_node) + 1);` 这句话中，如果 `near[cur]` 先执行得到引用 `const T &`，后执行 go 函数让 near 扩容从而使引用失效。

（为什么是“如果”，因为这两个部分的求值顺序是未指定的）
