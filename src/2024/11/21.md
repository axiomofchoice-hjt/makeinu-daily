---
date: 2024-11-21
next:
  link: 2024/11/22
  text: 败犬日报 2024-11-22
prev:
  link: 2024/11/20
  text: 败犬日报 2024-11-20
title: 败犬日报 2024-11-21
description: 本期日报包含 C++ 语言特性、编程实践和面试算法等多个话题
__tags__:
- title: '## 1. 函数里移动一个左值引用传入的参数是否合理'
  score: 8/10
  tags:
  - C++
  - 内存管理
  - 工程实践
  - 进阶知识
  explanation: 讨论在函数中移动左值引用参数的合理性，涉及移动语义和内存管理，属于 C++ 进阶知识。
- title: '## 2. `unique_ptr` 自定义删除器中是不是没有必要判断指针是否为 nullptr'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - 进阶知识
  explanation: 探讨 unique_ptr 自定义删除器中 nullptr 判断的必要性，涉及智能指针和内存管理，属于 C++ 进阶知识。
- title: '## 3. 面试题，求最大长度为 k 的最大子段和'
  score: 8/10
  tags:
  - 算法
  - 面试常见
  - 基础知识
  explanation: 介绍求解最大长度为 k 的子段和算法，使用前缀和和单调队列，属于算法基础知识和面试常见题。
- title: '## 4. 独立的两个类 A B，`A a; B* p = (B*)&a;` 是不是 UB'
  score: 7/10
  tags:
  - C++
  - 类型
  - 专家知识
  explanation: 讨论不同类型指针转换的 UB 问题，涉及类型系统和严格别名规则，属于 C++ 专家知识。
- title: '## 5. 给 lambda 加上 always inline'
  score: 8/10
  tags:
  - C++
  - 函数
  - 编译构建和调试
  - 性能优化
  - 进阶知识
  explanation: 介绍如何为 lambda 函数添加 always_inline 属性，涉及函数优化和编译器特性，属于 C++ 进阶知识。
- title: '## 6. 有什么不能 always_inline 的情形'
  score: 7/10
  tags:
  - C++
  - 编译构建和调试
  - 基础知识
  explanation: 列举不能使用 always_inline 的情形，如递归和函数指针，涉及编译器优化限制，属于 C++ 基础知识。
- title: '## 7. boost::json 性能和 rapidjson 接近，部分 payload 还能暴打'
  score: 8/10
  tags:
  - C++
  - 性能优化
  - 标准库特性
  - 进阶知识
  explanation: 比较 boost::json 和 rapidjson 的性能表现，涉及 JSON 解析库的性能优化，属于 C++ 进阶知识。
- title: '## 8. 不同 json 解析库行为可能不一致'
  score: 7/10
  tags:
  - C++
  - 标准库特性
  - 工程实践
  - 进阶知识
  - 需改进
  explanation: 指出不同 JSON 解析库在 corner case 处理上的不一致性，涉及工程实践，需要改进文档说明。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 函数里移动一个左值引用传入的参数是否合理

```cpp
void func(A &a) {
    funcB(std::move(a));
}
```

没问题的，注释要写清楚。

## 2. `unique_ptr` 自定义删除器中是不是没有必要判断指针是否为 nullptr

没有必要。

## 3. 面试题，求最大长度为 k 的最大子段和

线性复杂度的做法是，在前缀和上跑单调队列求长度等于 k 的区间最小值，用右端点减去这个最小值。

## 4. 独立的两个类 A B，`A a; B* p = (B*)&a;` 是不是 UB

不是 UB，解引用 p 才是 UB。

## 5. 给 lambda 加上 always inline

如果是 GCC/Clang：

```cpp
int x = 1;
__attribute__((always_inline)) inline void foo(int y) {
    x = y;
}
int main() {
    auto bar = [] __attribute__((always_inline)) (int y) {
        x = y;
    };

    foo(2);
    bar(3);
}
```

一般可以用这个宏定义：

```cpp
#if defined(__GNUC__) || defined(__clang__)
// GCC or Clang Compiler
#define ALWAYS_INLINE __attribute__((always_inline)) inline
#define ALWAYS_INLINE_LAMBDA __attribute__((always_inline))
#endif
```

## 6. 有什么不能 always_inline 的情形

递归，函数指针。

## 7. boost::json 性能和 rapidjson 接近，部分 payload 还能暴打

<https://230.jsondocs.prtest.cppalliance.org/libs/json/doc/html/json/benchmarks.html>

性能更好的考虑 simdjson。

## 8. 不同 json 解析库行为可能不一致

一些 corner case，比如转义字符不一致，nan 处理等。
