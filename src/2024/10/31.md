---
date: 2024-10-31
next:
  link: 2024/11/01
  text: 败犬日报 2024-11-01
prev:
  link: 2024/10/30
  text: 败犬日报 2024-10-30
title: 败犬日报 2024-10-31
description: 本期日报包含 Windows 时间获取、虚继承和模板实例化等 C++ 话题
__tags__:
- title: '## 1. windows cpp 获取当前时间，精确到秒，性能越高越好'
  score: 7/10
  tags:
  - C++
  - 性能优化
  - 硬件相关
  - 进阶知识
  explanation: 介绍在 Windows 平台使用 GetSystemTimePreciseAsFileTime API 获取高精度时间的方法，涉及性能优化和硬件相关特性，属于进阶知识。
- title: '## 2. 面试问到了虚基类（虚继承）'
  score: 8/10
  tags:
  - C++
  - 面向对象编程
  - 基础知识
  - 面试常见
  explanation: 讲解虚继承的语法、用途和实际应用场景（如菱形继承问题），涉及面向对象编程的核心概念，是面试常见的基础知识。
- title: '## 3. 模板类的实例化在成员函数定义前'
  score: 9/10
  tags:
  - C++
  - 泛型编程
  - 编译构建和调试
  - 专家知识
  - 需改进
  explanation: 分析模板类成员函数定义顺序对符号导出的影响，对比 GCC 和 Clang 的行为差异，引用 C++ 标准说明正确写法。涉及泛型编程和编译器实现细节，属于专家知识。需改进：可更详细解释标准条款内容。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. windows cpp 获取当前时间，精确到秒，性能越高越好

调 win api <https://learn.microsoft.com/zh-cn/windows/win32/api/sysinfoapi/nf-sysinfoapi-getsystemtimepreciseasfiletime>

## 2. 面试问到了虚基类（虚继承）

虚继承写法是 `class Derived: virtual public Base`

用于菱形继承，继承的多个 Base 最终只有一个 Base 实例。

如果 Base 是有状态的，虚继承就是必要的。比如 cocos2d 里的很多对象都继承自一个引用计数类，多继承就会导致引用计数模块重复，虚继承就可以解决这个问题。

## 3. 模板类的实例化在成员函数定义前

```cpp
template <typename T>
struct X {
    void foo();
};

template struct X<int>;

template <typename T>
void X<T>::foo() {}
```

上面代码 gcc 会导出 `X<int>::foo()` 符号，clang 不会。

```cpp
template <typename T>
struct X {
    void foo();
};

template <typename T>
void X<T>::foo() {}

template struct X<int>;
```

这样换个顺序就都会导出了。（并且这才是正确的写法）

似乎 clang 才是正确的 <https://eel.is/c++draft/temp.explicit#10>
