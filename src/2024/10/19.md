---
date: 2024-10-19
next:
  link: 2024/10/20
  text: 败犬日报 2024-10-20
prev:
  link: 2024/10/18
  text: 败犬日报 2024-10-18
title: 败犬日报 2024-10-19
description: 本期日报包含编译期类型判断的话题
__tags__:
- title: '## 1. 编译期判断一个类型为常指针'
  score: 8/10
  tags:
  - C++
  - 编译期
  - 泛型编程
  - 类型
  - 进阶知识
  explanation: 介绍如何使用 type_traits 和 concepts 在编译期判断一个类型是否为常指针，涉及编译期计算和泛型编程，属于进阶知识。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 编译期判断一个类型为常指针

```cpp
#include <type_traits>

template <typename T>
concept IsConstPointer =
    std::is_pointer_v<T> && std::is_const_v<std::remove_pointer_t<T>>;

template <typename T>
concept IsConstPointerV2 = requires {
    requires std::is_pointer_v<T>;
    requires std::is_const_v<std::remove_pointer_t<T>>;
};

// 没有 concept 可以这么写
template <typename T>
constexpr bool IsConstPointerV3 =
    std::is_pointer_v<T> && std::is_const_v<std::remove_pointer_t<T>>;

int main() {
    static_assert(IsConstPointer<const int*>);
    static_assert(!IsConstPointer<int>);
    static_assert(!IsConstPointer<const int>);
    static_assert(!IsConstPointer<int*>);
}
```
