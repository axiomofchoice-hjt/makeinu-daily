---
date: 2025-07-17
next:
  link: /2025/07/18
  text: 败犬日报 2025-07-18
prev:
  link: /2025/07/16
  text: 败犬日报 2025-07-16
title: 败犬日报 2025-07-17
description: 本期日报包含 C++ 内存管理、标准库特性、数据结构生命周期管理和语言学习推荐等话题
__tags__:
- title: '## 1. std::shared_ptr 基类指针有偏移，怎么正确释放'
  score: 8/10
  tags:
  - C++
  - 内存管理
  - 面向对象编程
  - 进阶知识
  explanation: 讨论 std::shared_ptr 在继承场景下的正确释放方式，涉及 Deleter 机制和内存管理，属于面向对象编程的进阶知识。
- title: '## 2. 图这样的数据结构怎么管理生命周期'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 工程实践
  - 基础知识
  explanation: 建议使用对象池统一管理所有权，配合裸指针实现图结构，涉及内存管理和工程实践的基础知识。
- title: '## 3. shared_ptr 别名构造函数'
  score: 6/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - 进阶知识
  explanation: 介绍 shared_ptr 别名构造函数的特性，允许不同类型共享引用计数，涉及标准库特性和内存管理的进阶知识。
- title: '## 4. 模式匹配提案'
  score: 6/10
  tags:
  - C++
  - 语法特性
  - 专家知识
  - 内容较主观
  explanation: 点评 C++ 模式匹配提案 P2688R5，涉及语法特性，属于专家知识范畴，内容较主观。
- title: '## 5. 学 C++ 推荐什么'
  score: 8/10
  tags:
  - C++
  - 基础知识
  - 内容较主观
  explanation: 推荐 C++ 学习资源，包括《现代C++32讲》和 CS106L 等，涉及基础知识，内容较主观。
- title: '## 6. `std::string::c_str()` 的值移动后失效'
  score: 7/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - STL
  - 进阶知识
  explanation: 分析 std::string 移动后 c_str() 失效的问题，涉及 SSO 优化和标准库特性，建议使用 std::hive 作为对象池，属于进阶知识。
---

# 败犬日报 2025-07-17

[[toc]]

## 1. std::shared_ptr 基类指针有偏移，怎么正确释放

[往期](/2025/07/16)提到，基类地址相对于子类有偏移，因此 std::shared_ptr 不一定指向子类。

答案是 std::shared_ptr 在初始化时已经决定了怎么释放；更详细地，shared_ptr 的 Deleter 是一个类似 std::function 的东西，它捕获了原始指针。

## 2. 图这样的数据结构怎么管理生命周期

所有权统一管理（用对象池），然后裸指针互相指。不要想花里胡哨的，该裸指针就裸指针。

## 3. shared_ptr 别名构造函数

不同类型，但是共享引用计数。

## 4. 模式匹配提案

<https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2025/p2688r5.html>

有点怪。

## 5. 学 C++ 推荐什么

《现代C++32讲》，或 CS106L +《C++ Core Guidelines》。

## 6. `std::string::c_str()` 的值移动后失效

```cpp
#include <cstdio>
#include <string>
#include <vector>

int main() {
    std::vector<std::string> a{"123"};
    auto p = a[0].c_str();
    printf("%s\n", a[0].c_str());  // 输出 123
    a.push_back("456");
    printf("%s\n", a[0].c_str());  // 输出 123
    printf("%s\n", p);             // 可能没有输出（GCC 15.1 不加编译参数）
    return 0;
}
```

SSO（小字符串优化）导致的。

按照标准的话，用来移动构造其他对象的 string，所有迭代器和指针都失效了。所以即使不满足 SSO 也不要这么用。

由此看出，用 `vector<string>` 当作 pool 容易触发这个问题，所以更好的实践是整个 `std::hive` 类似的对象池。

与之不同的是，`std::vector::data()` 的值移动后不会失效，<https://zh.cppreference.com/w/cpp/container/vector/vector.html> 的 Notes 章节。
