---
date: 2025-03-04
next:
  link: 2025/03/05
  text: 败犬日报 2025-03-05
prev:
  link: 2025/03/03
  text: 败犬日报 2025-03-03
title: 败犬日报 2025-03-04
description: 本期日报包含关于 C++ union 行为的两个话题
__tags__:
- title: '## 1. union 的默认初始化是什么行为'
  score: 8/10
  tags:
  - C++
  - 内存管理
  - 基础知识
  explanation: 介绍 union 的默认构造函数行为，说明只有 trivial 成员时才有默认构造函数，否则需要显式定义，属于内存管理的基础知识。
- title: '## 2. union 用不同的类型读写'
  score: 9/10
  tags:
  - C++
  - 内存管理
  - 类型
  - 进阶知识
  - 需改进
  explanation: 讨论 union 中不同类型读写的未定义行为，涉及严格别名规则和编译器扩展，属于内存管理和类型的进阶知识。需改进：建议补充更多实际示例和编译器具体行为的说明。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. union 的默认初始化是什么行为

union 只有当成员都 trivial 的时候才会有默认构造函数，效果是啥都不干。否则会报错，必须显式定义一个。

## 2. union 用不同的类型读写

```cpp
union { T x; U y; }
write(x)
read(y)
```

这个在 C++ 中是 UB（UB = 未定义行为）。

[往期](/2025/02/20)提到 <https://zh.cppreference.com/w/cpp/language/union> 有记载：

> 读取并非最近写入的联合体成员是未定义行为。许多编译器以非标准语言扩展实现读取联合体的不活跃成员的能力。

但是这个 UB 太常见了，编译器一般不会怎么样。但还是需要注意严格别名，不要把指针传播出去，比如：

```cpp
union A {
    int x;
    float y;
};
void foo(int*, float*);
void bar() {
    A a;
    a.x = 1;
    foo(&a.x, &a.y);
}
```
