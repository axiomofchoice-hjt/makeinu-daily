---
date: 2025-10-31
next:
  link: /2025/11/01
  text: 败犬日报 2025-11-01
prev:
  link: /2025/10/30
  text: 败犬日报 2025-10-30
title: 败犬日报 2025-10-31
description: 本期日报包含 C++ 模板参数推导、多线程性能优化和算法问题的话题
__tags__:
- title: '## 1. 随笔：在C++中像Python一样传递keyword argument（文章）'
  score: 6/10
  tags:
  - C++
  - 语法特性
  - 工程实践
  - 进阶知识
  - 搞笑
  - 内容较主观
  explanation: 探讨在 C++ 中模拟 Python 关键字参数传递的趣味方法，涉及语法特性和工程实践，内容较主观且带有搞笑性质，属于进阶知识。
- title: '## 2. 怎么给 `T&&` 参数一个回调函数默认参数'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 泛型编程
  - 编译期
  - 基础知识
  explanation: 讲解模板参数推导和默认参数的问题，提供 C++20 及之前的解决方案，涉及语法特性、泛型编程和编译期知识，属于基础知识。
- title: '## 3. 面试题：1024 核，N 个数找最大值，不许用锁或原子操作'
  score: 7/10
  tags:
  - C++
  - 并发编程
  - 性能优化
  - 硬件相关
  - 面试常见
  - 进阶知识
  - 内容较主观
  explanation: 分析多线程找最大值的面试题，讨论线程同步开销和优化策略，涉及并发编程、性能优化和硬件相关，内容较主观，属于进阶知识。
- title: '## 4. 面试题：n 个数 `x[i]` 要求移动后 `L <= x[i] <= R`，`x[i] + w < x[i + 1]`，求最小移动距离之和'
  score: 9/10
  tags:
  - 算法
  - 专家知识
  - 面试常见
  - 其他
  explanation: 介绍保序回归（单调回归）算法问题，将原问题转化为经典保序回归并讨论求解方法，涉及复杂算法，属于专家知识。
---

# 败犬日报 2025-10-31

[[toc]]

## 1. 随笔：在C++中像Python一样传递keyword argument（文章）

<https://zhuanlan.zhihu.com/p/1966608200796177222>

看一乐。

## 2. 怎么给 `T&&` 参数一个回调函数默认参数

```cpp
template <typename T>
void run(T &&callback = [](auto &&arg) {}) {
    callback(1);
}

int main() {
    run();  // Candidate template ignored: couldn't infer template argument 'T'
    run([](auto &&arg) {});
}
```

要指定默认模板参数，因为默认参数不参与模板推导。

```cpp
template <typename T = decltype([](auto &&) {})>
void run(T &&callback = {}) {
    callback(1);
}

int main() {
    run();
    run([](auto &&) {});
}
```

在 C++20 以前的写法是：

```cpp
struct default_functor {
    template <typename T>
    void operator()(T &&) const {}
};

template <typename T = default_functor>
void run(T &&callback = {}) {
    callback(1);
}

int main() {
    run();
    run([](auto &&) {});
}
```

## 3. 面试题：1024 核，N 个数找最大值，不许用锁或原子操作

这问题就很奇怪，线程同步的几个方式算不算用锁或原子变量？算就没法多线程了，那 1024 核的条件有什么用。

不考虑“不许用锁或原子操作”，就把 N 给分为 1024 个等长的块，每个线程处理一块，拿到每一块的最大值。最后单线程把每个块的最大值再求一遍最大值即可。

用 openmp 很容易就能实现：

```cpp
#include <omp.h>

#include <algorithm>
#include <cstdio>
#include <limits>
#include <vector>

int get_max(int *a, int n, int n_threads) {
    std::vector<int> result(n_threads, 0);
#pragma omp parallel num_threads(n_threads)
    {
        int max = std::numeric_limits<int>::min();
#pragma omp for
        for (int i = 0; i < n; i++) {
            max = std::max(max, a[i]);
        }
        result[omp_get_thread_num()] = max;
    }
    return *std::max_element(result.begin(), result.end());
}

int main() {
    int arr[] = {1, 3, 2, 8, 4, 6};
    printf("%d\n", get_max(arr, std::size(arr), 4));
}

// g++ test.cpp -o test -O3 -fopenmp && ./test
```

这里有一点是最后一步为什么不用多线程？这是因为 1024 个数求最大值已经非常快了，而且数据在 L3 cache 上，粗略估计 100 ns。而线程同步开销本身就至少几百 ns，在我机器上 4 核同步 600 ns。

所以单线程就是最快的。

用下面的程序可以测一下线程同步开销：

```cpp
#include <omp.h>

#include <chrono>
#include <iostream>

int main() {
    const int STEP = 1000000;
    const int n_threads = 4;
    auto start = std::chrono::high_resolution_clock::now();
    for (int i = 0; i < STEP; ++i) {
#pragma omp parallel num_threads(n_threads)
        {
            asm volatile("" ::: "memory");
        }
    }
    auto end = std::chrono::high_resolution_clock::now();
    auto elapsed = end - start;
    std::cout << "time: " << elapsed.count() / STEP << " ns\n";
}
```

但是群友表示这个答案面试官认为还能再快，那就再把 simd，numa，分布式，gpu 这些答一遍，总该满意了。

## 4. 面试题：n 个数 `x[i]` 要求移动后 `L <= x[i] <= R`，`x[i] + w < x[i + 1]`，求最小移动距离之和

这题作为面试题难度太大了，是保序回归（又叫单调回归）。首先令 `y[i] = x[i] + (w + 1) * i`，这样条件就简化为 `L <= y[i] <= R + (w + 1) * (n - 1)`，`y[i] <= y[i + 1]`。这个就是经典的保序回归了，套模板即可。

保序回归：给定实数序列 $a_i$，求不下降实数序列 $b_i$，使得 $\displaystyle\sum_{i=1}^{n}|a_i-b_i|^p$ 最小化。

p = 1 就是这题，需要单调栈 + 维护中位数（双堆或平衡树）。因为一堆点移动到一个点是中位数最优，保序回归就把数组分成多个块，每块求中位数即可。
