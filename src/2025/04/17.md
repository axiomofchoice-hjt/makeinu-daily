---
date: 2025-04-17
next:
  link: 2025/04/18
  text: 败犬日报 2025-04-18
prev:
  link: 2025/04/16
  text: 败犬日报 2025-04-16
title: 败犬日报 2025-04-17
description: 本期日报包含 C++ 变量模板特化、priority_queue 使用限制和 CMake 工具吐槽三个话题
__tags__:
- title: '## 1. 变量模板全特化不能有 thread_local 之外的存储说明符'
  score: 9/10
  tags:
  - C++
  - 语法特性
  - 泛型编程
  - 专家知识
  - 冷门知识
  explanation: 讨论 C++ 变量模板全特化的存储说明符限制，指出只能使用 thread_local，不能使用 extern 或 static，属于语法特性和泛型编程的专家级冷门知识。
- title: '## 2. `priority_queue<unique_ptr<T>>` push 进去就出不来了'
  score: 8/10
  tags:
  - C++
  - 标准库特性
  - STL
  - 内存管理
  - 进阶知识
  - 需改进
  explanation: 分析 priority_queue 与 unique_ptr 结合使用时无法移动元素的问题，指出标准库设计限制，属于 STL 和内存管理的进阶知识，但内容不够全面，未提供完整解决方案。
- title: '## 3. cmake 这东西有大毒'
  score: 7/10
  tags:
  - 编译构建和调试
  - 工程实践
  - 内容较主观
  - 其他
  explanation: 吐槽 CMake 工具的文档和使用体验，主观性强，涉及编译构建和工程实践，属于其他类别。
---

# 败犬日报 2025-04-17

[[toc]]

## 1. 变量模板全特化不能有 thread_local 之外的存储说明符

主要是 extern，static 这两个存储说明符。

```cpp
template<typename T> extern T value;  // 主模板声明
template<typename T> extern T value<T*>;  // 偏特化声明
template<> extern int value<int>;  // 全特化声明（GCC 报错，Clang 警告）
```

因为标准不让这么做。<http://eel.is/c++draft/temp.expl.spec#2>

看起来是当初存储说明符考虑的只有类模板静态数据成员，变量模板一直没加，后来只是多允许了 thread_local。

解决办法很显然，用类模板静态数据成员。

## 2. `priority_queue<unique_ptr<T>>` push 进去就出不来了

没错，`top()` 返回值是 `const T&` 无法移动，`pop()` 又没有返回值。

如果强行用 const_cast 并移动出来，就破坏堆的性质了。`pop()` 等函数要满足堆的性质，所以是未定义行为。

建议用 `std::make_heap` 代替 `priority_queue`。

## 3. cmake 这东西有大毒

文档看了和没看一样，问 AI 给出错误答案。

一个 api 十万个功能，点名批评 FILE。
