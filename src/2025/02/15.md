---
date: 2025-02-15
next:
  link: 2025/02/16
  text: 败犬日报 2025-02-16
prev:
  link: 2025/02/14
  text: 败犬日报 2025-02-14
title: 败犬日报 2025-02-15
description: 本期日报包含 C++ 内存序相关的话题，涉及内存模型、原子操作、并发编程等内容
__tags__:
- title: '## 1. 内存序先写后读的问题'
  score: 9/10
  tags:
  - C++
  - 并发编程
  - 内存管理
  - 专家知识
  - 硬件相关
  explanation: 分析 C++ 内存序中 acquire-release 语义的局限性，解释为何不同变量的先写后读可能被重排序，需要使用 seqcst
    或 memory fence 来解决，属于并发编程的专家知识。
- title: '## 2. 内存序程序出了 bug，定位起来可太难了'
  score: 7/10
  tags:
  - C++
  - 并发编程
  - 专家知识
  - 需改进
  explanation: 提醒内存序编程的调试难度，但内容过于简略，缺乏具体案例和建议，需要改进补充。
- title: '## 3. 不同架构上的内存序'
  score: 8/10
  tags:
  - C++
  - 并发编程
  - 硬件相关
  - 专家知识
  explanation: 提供不同 CPU 架构下 C++ 内存序的映射关系参考链接，涉及硬件相关的内存模型实现，属于专家知识。
- title: '## 4. 内存序如果是运行时的值'
  score: 8/10
  tags:
  - C++
  - 并发编程
  - 编译构建和调试
  - 专家知识
  explanation: 说明运行时内存序参数在 GCC 中的处理方式（按 seqcst 处理），涉及编译器实现细节，属于专家知识。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 内存序先写后读的问题

```cpp
std::atomic<int> done;
std::atomic<int> waiting;

void notify() {
    done = 1;
    if (!waiting)
        return;
    // ... wake up waiter...
}

void wait() {
    waiting = 1;
    if (done)
        return;
    // ... sleep...
}
```

假如 done, waiting 读的内存序是 acquire，写是 release。并发调用 notify 和 wait，为什么有可能出现 notify 立刻返回且 wait 陷入 sleep？

因为内存序的定义是这样的。acquire 阻止之后的指令排到前面，release 阻止之前的指令排到后面。但是先写后读不同的变量（同一变量有保障），不会阻止它们的乱序成先读后写。

所以这里需要 seqcst 内存序，也可以用 std::atomic_thread_fence。

## 2. 内存序程序出了 bug，定位起来可太难了

一般不要碰。

## 3. 不同架构上的内存序

<https://www.cl.cam.ac.uk/~pes20/cpp/cpp0xmappings.html>

## 4. 内存序如果是运行时的值

一般会按照 seqcst 处理。

<https://gcc.gnu.org/onlinedocs/gcc/_005f_005fatomic-Builtins.html> 有记载：

> These built-in functions map any run-time value to `__ATOMIC_SEQ_CST` rather than invoke a runtime library call or inline a switch statement.
