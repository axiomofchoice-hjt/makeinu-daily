---
date: 2025-02-16
next:
  link: 2025/02/17
  text: 败犬日报 2025-02-17
prev:
  link: 2025/02/15
  text: 败犬日报 2025-02-15
title: 败犬日报 2025-02-16
description: 本期日报包含 C++ 内存序、编译器实现和栈帧等话题
__tags__:
- title: '## 1. 会议还通过了 P3475（废除没人能懂的 memory_order::consume）'
  score: 9/10
  tags:
  - C++
  - 并发编程
  - 专家知识
  - 冷门知识
  - 需改进
  explanation: 讨论 C++ 内存模型中 memory_order::consume 的废除原因，解释 consume-rel 同步机制及其实现困难，属于并发编程的专家知识。需改进：内容可以更全面地解释数据依赖的具体例子。
- title: '## 2. clang parse 的使用'
  score: 7/10
  tags:
  - C++
  - 编译构建和调试
  - 专家知识
  - 其他
  explanation: 介绍 clang 解析器的使用，涉及编译器内部实现，属于编译构建和调试的专家知识。
- title: '## 3. 写 cpp parser 几乎不可能'
  score: 6/10
  tags:
  - C++
  - 编译构建和调试
  - 专家知识
  - 内容较主观
  explanation: 讨论编写 C++ 解析器的难度，基于 clang 代码规模说明，属于编译构建和调试的专家知识。内容较主观：观点基于代码行数，未考虑其他实现方式。
- title: '## 4. 栈帧的大小是编译期决定的吗'
  score: 8/10
  tags:
  - C++
  - 内存管理
  - 基础知识
  - 硬件相关
  explanation: 解释栈帧大小的决定时机，讨论 VLA 和 alloca 的影响，涉及内存管理和硬件相关的基础知识。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 会议还通过了 P3475（废除没人能懂的 memory_order::consume）

这是一个比 acq 弱的内存序。

acq-rel 同步是如果 acq 读到了 rel 的影响，那么 rel 前面的都对 acq 后面的可见。consume-rel 同步是如果 consume 读到了 rel 的影响，那么 consume 前数据依赖于被 rel 写入的值的那些数据都对 consume 后面的操作可见。consume 只限制前面 rel 对写入值有数据依赖的部分，acq 则是 rel 前面所有部分。

比如拿单链表搓个栈，流程是先 new 节点，将节点的 nxt 指向头，再 cas。这时 cas 写入的是 new 出来的节点，而节点的 nxt 指向头这部必须对后面读到这个新节点的 pop 操作可见。

***

为什么删除：<<https://wg21.link/p3475>>

一个原因是实现起来非常困难，导致现有的编译器都用 acq 来实现 consume。

## 2. clang parse 的使用

深入探索 clang（上） - YKIKO的文章 - 知乎

<https://zhuanlan.zhihu.com/p/21319978959>

## 3. 写 cpp parser 几乎不可能

clang 的代码不算测试有 100 万行。

## 4. 栈帧的大小是编译期决定的吗

栈帧是一个函数使用的栈空间。

不考虑 C 的 VLA（变长数组）和 alloca 的话，一个函数使用的栈空间可以被计算出来，完全可以在编译期决定。
