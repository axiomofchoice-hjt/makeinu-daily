---
date: 2025-02-28
next:
  link: 2025/03/01
  text: 败犬日报 2025-03-01
prev:
  link: 2025/02/27
  text: 败犬日报 2025-02-27
title: 败犬日报 2025-02-28
description: 本期日报包含 C++ 单元测试、SFINAE 和智能指针相关话题
__tags__:
- title: '## 1. 单元测试怎么测 private 函数'
  score: 8/10
  tags:
  - C++
  - 工程实践
  - 基础知识
  - 内容较主观
  explanation: 讨论单元测试中测试 private 函数的方法，推荐测试接口而非实现，提供 -fno-access-control 作为备选方案，涉及工程实践和基础知识。
- title: '## 2. 可不可以写一个程序，开不开 -fno-access-control 行为会不同'
  score: 7/10
  tags:
  - C++
  - 语法特性
  - 泛型编程
  - 进阶知识
  explanation: 探讨如何利用 SFINAE 编写对 -fno-access-control 编译选项敏感的程序，涉及语法特性和泛型编程，属于进阶知识。
- title: '## 3. unique_ptr 的大小'
  score: 9/10
  tags:
  - C++
  - 内存管理
  - 标准库特性
  - 基础知识
  explanation: 分析 unique_ptr 在不同 Deleter 情况下的内存大小，解释空基类优化原理，涉及内存管理和标准库特性，属于基础知识。
---

# {{ $frontmatter.title }}

[[toc]]

## 1. 单元测试怎么测 private 函数

好的实践是测接口功能而不是内部实现。如果你觉得你特别需要测 private 函数，很有可能意味着需要修改设计，把复杂的逻辑单独提出来一个工具类。

如果一定要测，就用 `-fno-access-control`（不要用 `#define private public` 给代码下毒）。

推荐阅读 <https://google.github.io/googletest/advanced.html#testing-private-code>。

## 2. 可不可以写一个程序，开不开 `-fno-access-control` 行为会不同

用 SFINAE。

## 3. `unique_ptr` 的大小

如果 Deleter 是默认的，或者是空类，大小是 8（一个指针的大小）。在实现上可以用空基类优化把这个 Deleter 空间搞掉。

```cpp
#include <iostream>
#include <memory>

struct A {
    void operator()(int*) {}
};

int main() {
    std::unique_ptr<int, A> a;
    std::tuple<int*, A> b;
    std::cout << sizeof(a) << '\n';  // 输出 8
    std::cout << sizeof(b) << '\n';  // 输出 8
}
```
