---
date: 2025/11/28
title: 败犬日报 2025-11-28
next:
  link: /2025/11/29
  text: 败犬日报 2025-11-29
prev:
  link: /2025/11/27
  text: 败犬日报 2025-11-27
description: 本期日报包含 C++ 预处理指令、异常机制和命名空间使用等话题
__tags__:
- title: '## 1. `#pragma once` 的作用是允许菱形 include 通过编译，这会导致环形 include 也能编译'
  score: 7/10
  tags:
  - C++
  - 预处理和宏
  - 基础知识
  - 需改进
  explanation: 讨论 `#pragma once` 的作用及其副作用（允许环形 include 编译），并提及使用 clang-tidy 检查环形 include。标签理由：涉及
    C++ 预处理指令，属于基础知识。需改进：标题表述有误导性，`#pragma once` 的主要作用是防止同一头文件被多次包含，其副作用是可能掩盖环形 include
    的错误，而非‘允许菱形 include 通过编译’是其‘作用’。建议修正表述。
- title: '## 2. `#pragma once` 和 include guard 用哪个'
  score: 8/10
  tags:
  - C++
  - 预处理和宏
  - 工程实践
  - 基础知识
  - 内容较主观
  explanation: 对比 `#pragma once` 和传统 include guard 的优缺点，并表达了倾向于使用 `#pragma once` 的个人观点。标签理由：涉及
    C++ 预处理和代码组织，属于工程实践和基础知识。内容主观性强，因为选择哪种方式常取决于团队偏好。
- title: '## 3. 任何东西都能 throw'
  score: 6/10
  tags:
  - C++
  - 错误处理
  - 基础知识
  - 需改进
  explanation: 指出 C++ 中 throw 可以抛出任意类型，而不仅限于 std::exception 派生类，并评论这可能是一种设计哲学但也容易导致问题。标签理由：涉及
    C++ 异常机制，属于错误处理的基础知识。需改进：解释过于简略，没有说明这种设计带来的具体问题（如异常安全、catch 时的类型处理）或最佳实践，建议补充。
- title: '## 4. 关于怎么 using 导入库的内容'
  score: 9/10
  tags:
  - C++
  - 工程实践
  - 基础知识
  - 内容较主观
  explanation: 讨论在 C++ 中使用 using 导入命名空间内容的策略，推荐使用命名空间别名或展开最后一层命名空间，并反对在源文件中直接 using
    单个类型名。标签理由：涉及 C++ 代码组织和可读性，属于工程实践和基础知识。内容包含了明确的个人偏好和建议。
---

# 败犬日报 2025-11-28

[[toc]]

## 1. `#pragma once` 的作用是允许菱形 include 通过编译，这会导致环形 include 也能编译

这个标题也算一个八股知识点。

另外检查环形 include 可以用 clang-tidy 扫描 misc-header-include-cycle。

## 2. `#pragma once` 和 include guard 用哪个

投票 `#pragma once` 的应该更多。虽然这个不是标准的，但是各大编译器都支持，且简短。

而 include guard 要维护宏名，还包括构建时生成的文件，非常麻烦。

## 3. 任何东西都能 throw

throw 和 std::exception 是解耦的，这可能是一种哲学。

但也是挖坑。

## 4. 关于怎么 using 导入库的内容

个人更倾向于展开最后一层命名空间。例如：

```cpp
namespace A::B::C {
    void f();
}

namespace C {
    using namespace A::B::C;
}
// 或者
namespace C = A::B::C;
```

`namespace fs = std::filesystem;` 这样的经典写法就是这个方式。

我认为 `using std::string;` 的写法即使在源文件里也应该避免。如果头文件 struct 里 `std::string`，源文件又是 string，不能确定是不是同一个东西，会增加心智负担。
