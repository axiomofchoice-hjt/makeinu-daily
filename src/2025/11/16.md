---
date: 2025/11/16
title: 败犬日报 2025-11-16
next:
  link: /2025/11/17
  text: 败犬日报 2025-11-17
prev:
  link: /2025/11/15
  text: 败犬日报 2025-11-15
description: 本期日报包含 C++ 右值引用和 std::vector<std::atomic<T>> 的话题
__tags__:
- title: '## 1. 右值引用传参时发生了什么'
  score: 8/10
  tags:
  - C++
  - 语法特性
  - 内存管理
  - 基础知识
  explanation: 解释 C++ 中右值引用传参时 prvalue 到 xvalue 的转换过程，涉及临时量实质化和 std::move 的使用，属于语法特性和内存管理的基础知识。
- title: '## 2. `std::vector<std::atomic<T>>` 会发生什么'
  score: 7/10
  tags:
  - C++
  - STL
  - 并发编程
  - 语法特性
  - 进阶知识
  explanation: 分析 std::vector 存储 std::atomic 类型时的限制，由于 std::atomic 不可复制移动，导致 push_back
    和 resize 等操作编译失败，涉及 STL、并发编程和语法特性，属于进阶知识。
---

# 败犬日报 2025-11-16

[[toc]]

## 1. 右值引用传参时发生了什么

```cpp
#include <utility>

void foo(int&&) {}

int bar() { return 0; }

int main() {
    foo(114);
    foo(bar());
    int x = 514;
    foo(std::move(x));
}
```

`foo(114)` 中的 114 是一个 prvalue（纯右值），先发生了临时量实质化，实质化是指 prvalue 转换为了 xvalue（将亡值）。然后传给 foo。

`foo(bar())` 的 `bar()` 也是 prvalue，发生的事情同上。

`foo(std::move(x))` 里的 std::move 将 lvalue（左值）转换为 xvalue，然后传给 foo。

## 2. `std::vector<std::atomic<T>>` 会发生什么

```cpp
#include <atomic>
#include <vector>

int main() {
    std::vector<std::atomic<int>> a(10);
    a[0] = 1;
    a.pop_back();
    a.clear();
    // a.push_back(2);  // 编译报错
    // a.resize(2);     // 编译报错
}
```

可以用但有限制。

我们知道 std::atomic 不可复制不可移动。由于 push_back 和 resize 之类的操作会导致 vector 扩容，就需要复制或移动元素，所以这些操作都会编译失败。
